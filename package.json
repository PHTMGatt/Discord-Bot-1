{
  "name": "discord-webhook-bot",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "echo \"no build step needed\"",
    "render-build": "npm install && npm run build",
    "restart": "npm stop --if-present && npm start"
  },
  "dependencies": {
    "discord-webhook-bot": "file:",
    "dotenv": "^16.3.1",
    "express": "^4.21.2",
    "node-fetch": "^2.6.7",
    "openai": "^4.100.0"
  }
}
